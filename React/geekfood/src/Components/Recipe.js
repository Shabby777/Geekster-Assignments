import React from 'react';

const Recipe = ({ recipe }) => {
  return (
   <div className="recipe">
    //   <h2>{recipe.name}</h2>
    //   <p>Prep Time: {recipe.prep_time_minutes} minutes</p>
    //   <p>Cook Time: {recipe.cook_time_minutes} minutes</p>
    //   <p>Servings: {recipe.num_servings}</p>
    //   {/* You can add more details here based on the structure of the recipe object */}
    <h1>Add</h1>
    </div>
  );
};

export default Recipe;